---
"@llamaindex/liteparse": minor
---

Add Codex-backed OCR tooling with `lit codex-ocr`, `lit codex-ocr-server`, and `lit codex-ocr-pipeline`. The server preserves the standard LiteParse `/ocr` contract while exposing richer `/ocr/analyze` artifacts with page Markdown, metadata, layout regions, segmented assets, annotations, and provenance.
